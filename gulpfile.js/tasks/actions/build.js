/**
 * task: build
 * - generate dev/ with pipeline tasks
 * - --dist -> generate dist/ with pipline and dist tasks
 */

//----------------------------------------------------------
// modules
//----------------------------------------------------------
// minimist
