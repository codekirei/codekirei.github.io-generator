// metalsmith?
// minimize

/**
 * NEEDS TO HAVE:
 * - frontmatter parsing
 * - trianglify for title
 * - markdown rendering
 * - jade rendering
 * - aggregate blog page generation
 * - aggregate tag page generation
 * - pagination / infinite scrolling for aggregate blog page
 * - creation of permalinks/slugs (speakingurl?)
 * - reading time calculation and insertion
 */
